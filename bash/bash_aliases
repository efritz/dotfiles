#!/usr/bin/env bash

# Allow aliases to be used with sudo
alias sudo="sudo "

# Sane default flags
alias rm='rm -i'
alias mv='mv -i'
alias cp='cp -i'
alias ls='ls -GFh'
alias grep='grep --color=auto'

# Actual aliases
alias real='cd `pwd -P`'
